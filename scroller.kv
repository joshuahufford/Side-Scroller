#:kivy 1.10.0
#:import hex kivy.utils.get_color_from_hex

<ScrollerGame>:
	canvas:
		Color:
			rgba: hex('#87CEEB')
		Rectangle:
			size: self.size
			pos: self.pos

<Picture>:
	canvas:
		Color:
			rgb: (1,1,1)
		Rectangle:
			texture: self.texture
			size: self.size
			pos: self.pos